# URL of the Express API (rewrite target in next.config.js)
API_URL=http://localhost:3000

# Exposed to the browser — used for direct fetch calls (SSE, FormData uploads)
NEXT_PUBLIC_API_URL=http://localhost:3000
